services:
  bot:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: noirbot:local
    container_name: telegramBot
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "8080:8080"
